#include "task.h"

status_type activate_task(task_type id)
{
    status_type result = STATUS_OK;

    return result;
}

status_type terminate_task(void)
{
    status_type result = STATUS_OK;

    return result;
}